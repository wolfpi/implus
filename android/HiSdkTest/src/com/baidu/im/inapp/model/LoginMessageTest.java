package com.baidu.im.inapp.model;

import junit.framework.Assert;
import android.test.AndroidTestCase;

import com.baidu.im.frame.utils.ParcelableUtil;
import com.baidu.im.sdk.LoginMessage;

public class LoginMessageTest extends AndroidTestCase {

    public void testLoginMessage() {

    	
/*        LoginMessage loginMessage = new LoginMessage();
        loginMessage.setAccount("account");
        loginMessage.setPassword("password");
        loginMessage.setBduss("bduss");

        byte[] data = ParcelableUtil.marshall(loginMessage);
        LoginMessage newModel = ParcelableUtil.unmarshall(data, LoginMessage.CREATOR);

        Assert.assertNotNull(newModel);
        Assert.assertEquals(newModel.getAccount(), loginMessage.getAccount());
        Assert.assertEquals(newModel.getPassword(), loginMessage.getPassword());
        Assert.assertEquals(newModel.getBduss(), loginMessage.getBduss());*/

    }
}
